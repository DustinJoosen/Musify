@model List<Song>

<h3>Songs</h3>

<div class="explore-table mt-4">
    @if (Model.Any())
    {
        <table class="table table-striped table-bordered table-sm">
            <thead class="thead-dark">
                <tr class="table-header">
                    <th width="80">Actions</th>
                    <th>Title</th>
                    <th width="200">Artist</th>
                    <th width="100">Duration</th>
                    <th width="60">Add</th>
                </tr>
            </thead>
            @foreach (Song song in Model)
            {
                <tr class="min-width">
                    <td style="display: flex">
                        <button class="songlikebtn"><i class="fa-solid fa-heart"></i></button>
                        <button class="songplaybtn"><i class="fa-solid fa-play"></i></button>
                    </td>
                    <td>
                        <a asp-controller="Details" asp-action="Albums" asp-route-id="@song.Id" >@song.Title</a>
                    </td>
                    <td>@song.Artist.Name</td>
                    <td>@song.FormattedDuration</td>
                    <td>
                        <button class="btn btn-light">Add</button>
                    </td>
                </tr>
            }
        </table>
    }
    else
    {
        <p>No songs were found.</p>
    }
</div>
