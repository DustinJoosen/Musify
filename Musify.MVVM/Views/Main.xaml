<Window x:Class="Musify.MVVM.Views.Main"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Musify.MVVM.Views"
        xmlns:utility="clr-namespace:Musify.MVVM.Utility"
        mc:Ignorable="d"
        Icon="../../Lib/icon.png"
        Background="#e9e9e9"
        Title="Musify" Height="380" Width="650">
    <Window.Resources>
        <Style TargetType="Grid">
            <Setter Property="Background" Value="White" />
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="110"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Border BorderBrush="Black" BorderThickness="2 2 0 2" Grid.Column="0">
            <ContentControl x:Name="navControl" />
        </Border>
        <Border BorderBrush="Black" BorderThickness="2" Grid.Column="1">
            <ContentControl x:Name="contentControl" />
        </Border>
    </Grid>
</Window>
